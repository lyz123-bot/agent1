# 🧠 AI Agent 全栈多模态平台 · 技术学习思维导图

> 基于《项目说明文档.md》整理 | 生成日期：2026-09-01

```text
🧠 AI Agent 全栈多模态平台 · 技术学习思维导图
│
├── 🐍 Python Web 后端
│   ├── Django 5
│   │   ├── MVT 分层思想（Model-View-Template）
│   │   ├── ORM 与模型设计（AbstractUser 扩展、外键、UUID 主键）
│   │   ├── settings 配置（apps、media、static、中间件）
│   │   └── 多 App 模块化拆分
│   ├── Django REST Framework
│   │   ├── APIView 与请求/响应
│   │   ├── Serializer（序列化、校验、嵌套）
│   │   ├── 认证与权限类（IsAuthenticated / 自定义 admin 权限）
│   │   └── 流式响应 StreamingHttpResponse（SSE）
│   ├── 认证与安全
│   │   ├── JWT 原理（Header.Payload.Signature）
│   │   ├── access/refresh 双 token 与刷新流程
│   │   └── CORS 跨域（django-cors-headers）
│   └── API 文档
│       ├── OpenAPI 3.0 规范
│       └── drf-spectacular + Swagger UI
│
├── 🗄️ 数据与异步
│   ├── 数据库（SQLite → 进阶 PostgreSQL）
│   │   └── Django 迁移 migrations
│   └── Celery 异步任务
│       ├── 生产者-消费者模型、Broker（Redis/RabbitMQ）
│       ├── task 定义与 .delay() 调用、task_id 追踪
│       └── 开发环境 eager 同步执行模式
│
├── 📚 RAG 检索增强生成（本项目核心）
│   ├── 知识库构建（离线）
│   │   ├── 文档解析：PDF / DOCX / TXT / PPT
│   │   ├── 文本切块：RecursiveCharacterTextSplitter
│   │   │   ├── chunk_size / chunk_overlap 权衡
│   │   │   └── 为什么切块影响检索质量
│   │   ├── 嵌入模型：BGE-M3（sentence-transformers）
│   │   │   ├── 文本 → 向量（语义空间）
│   │   │   └── HuggingFace 模型加载与管理
│   │   └── 向量入库：ChromaDB
│   │       ├── collection、cosine 距离
│   │       └── metadata（source/chunk_id/doc_hash）与去重
│   ├── 检索（在线）
│   │   ├── 查询向量化 → 相似度召回
│   │   ├── 距离阈值过滤（MAX_DIST=0.5 兜底）
│   │   ├── MMR 最大边际相关性（λ=0.5）
│   │   │   └── 相关性与多样性平衡
│   │   └── 重排序 CrossEncoder（bge-reranker）
│   │       └── 交叉编码器 vs 双编码器原理区别
│   ├── 生成
│   │   ├── Prompt 工程（QA_PROMPT 模板、历史上下文注入）
│   │   ├── DeepSeek API（OpenAI 兼容接口）
│   │   ├── 流式输出 SSE（text/event-stream）
│   │   └── 效果观测（latency / tokens / source_documents 记录）
│   └── 进阶方向
│       ├── 混合检索（BM25 + 向量）
│       ├── 多路召回与 RRF 融合
│       └── RAG 评测（忠实度、召回率）
│
├── 👁️ 计算机视觉与多模态
│   ├── 传统 CV：OpenCV
│   │   ├── 特征点检测：SIFT / ORB 原理与区别
│   │   ├── 描述子匹配（特征点对筛选）
│   │   └── 离线特征缓存 + 在线匹配的架构模式
│   ├── 深度学习检测：RF-DETR
│   │   ├── DETR 架构（Transformer + 二分图匹配）
│   │   ├── COCO 80 类别、置信度阈值
│   │   └── supervision 库（画框、标注可视化）
│   └── 多模态大模型：DeepSeek Vision
│       ├── 图像描述 Image Captioning
│       ├── 视觉问答 VQA（图 + 文 → 文）
│       └── 多模态输入的 Prompt 组织方式
│
├── ⚡ 前端工程
│   ├── Vue 3
│   │   ├── 组合式 API（setup / ref / computed）
│   │   └── 组件化拆分（列表/气泡/输入框各司其职）
│   ├── Vite 7
│   │   ├── 极速构建与 HMR
│   │   └── dev server 代理解决跨域
│   ├── vue-router 4
│   │   └── 全局守卫 beforeEach + 路由级权限（meta: requireAuth/requireRole）
│   ├── Pinia
│   │   ├── user 状态管理
│   │   └── 与 localStorage 配合持久化 token
│   ├── HTTP 层：fetch 封装
│   │   └── 401 拦截 → 自动刷新 token → 重试原请求
│   └── UI 与渲染
│       ├── Ant Design Vue / Naive UI
│       └── marked + highlight.js（Markdown/代码高亮）
│
└── 🚀 工程化与进阶
    ├── 环境变量管理（.env、python-dotenv）
    ├── 前后端分离部署（后端 gunicorn + 前端静态托管/Nginx）
    ├── 向量库与模型权重的持久化与版本管理
    └── 学习路线建议
        ├── 阶段1：Django + DRF + JWT（先打通认证）
        ├── 阶段2：Vue 3 + Vite + Pinia（学会联调）
        ├── 阶段3：RAG 全链路（Embedding → 向量库 → 检索 → Prompt → LLM）
        ├── 阶段4：视觉方向二选一（OpenCV 传统 CV 或 DETR 检测）
        └── 阶段5：Celery 异步化 + 部署上线
```
